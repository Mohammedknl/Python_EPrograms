# Create a tuple of exam scores for a student
exam_scores = (84, 76, 91)

# Calculate the average score
average_score = sum(exam_scores) / len(exam_scores)

# Print the average score
print("The student's average score is: {:.2f}".format(average_score))
